using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TurnSystem : MonoBehaviour
{
    //virtual ~TurnSystem(){}
    //virtual void handleInput(Turn& Turn, Input input) {}
   // virtual void update(Turn& Turn)};
}
